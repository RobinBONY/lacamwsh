---
import camLogo from "../assets/logo.svg?raw";
import { localizePath, getT, DEFAULT_LOCALE, type Locale } from "../i18n/utils";

interface Props {
  locale?: Locale;
}

const { locale = DEFAULT_LOCALE } = Astro.props;
const t = getT(locale);
---

<a
  href={localizePath("/", locale)}
  class="nav-cell nav-btn logo-cell"
  aria-label={t("nav.home")}
  data-astro-prefetch
>
  <div class="logo-svg" set:html={camLogo} />
</a>

<style>
  .logo-cell {
    text-decoration: none;
  }
  .logo-svg :global(svg) {
    height: 1.4rem;
    width: auto;
    display: block;
    color: currentColor;
  }
  .logo-svg :global(svg path),
  .logo-svg :global(svg g) {
    fill: currentColor !important;
  }
  @media (min-width: 768px) {
    .logo-svg :global(svg) {
      height: 1.6rem;
    }
  }
</style>
