---
interface Props {
  class?: string;
}
const { class: className = "" }: Props = Astro.props;
---

<p class:list={["font-display text-[clamp(1rem,1.4vw,1.25rem)] font-medium leading-[1.6] m-0 mb-6", className]}>
  <slot />
</p>
